<%= form_with(model: purchase, html: { class: 'form-group d-flex flex-column gap-3' }) do |form| %>
  <% if purchase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
        <% purchase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nome", style: "display: block" %>
    <%= form.text_field :name, class: 'form-control-lg' %>
  </div>

  <div>
    <%= form.label "Descrição", style: "display: block" %>
    <%= form.text_area :description, class: 'form-control-lg' %>
  </div>

  <div>
    <%= form.label "Data da compra", style: "display: block" %>
    <%= form.date_field:due_date, class: 'form-control-lg' %>
  </div>

  <div>
    <%= form.submit action_name, class: 'btn btn-primary' %>
  </div>
<% end %>
