<%= form_with(model: task_list, html: {class:'form-group d-flex flex-column gap-3'}) do |form| %>
  <% if task_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task_list.errors.count, "error") %> prohibited this task_list from being saved:</h2>

      <ul>
        <% task_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nome", style: "display: block" %>
    <%= form.text_field :name, class:'form-control-lg' %>
  </div>

  <div>
    <%= form.label "Descrição", style: "display: block" %>
    <%= form.text_area :description, class:'form-control-lg' %>
  </div>
  <div>
    <%= form.submit action_name, class:'btn btn-primary' %>
  </div>
<% end %>
