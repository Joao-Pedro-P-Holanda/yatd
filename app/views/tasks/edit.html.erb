<% content_for :title, "Editar tarefa" %>
<meta name="turbo-visit-control" content="reload">


<h1>Editar tarefa</h1>

<%= render "form", task: @task, action_name: "Editar" %>

<br>

<div>
  <%= link_to "Voltar para lista de tarefas", task_list_path(@task_list), class: 'btn btn-secondary' %>
</div>
